import type { ToolConfig, ToolResult } from "@/backend/tools/calculation-tools";

export type AgeToolInput = Record<string, unknown>;
export type AgeToolConfig = ToolConfig;
export type AgeToolResult = ToolResult;
