import type { ToolConfig, ToolResult } from "@/backend/tools/calculation-tools";

export type ScientificToolInput = Record<string, unknown>;
export type ScientificToolConfig = ToolConfig;
export type ScientificToolResult = ToolResult;
