import type { ToolConfig, ToolResult } from "@/backend/tools/calculation-tools";

export type LoanToolInput = Record<string, unknown>;
export type LoanToolConfig = ToolConfig;
export type LoanToolResult = ToolResult;
