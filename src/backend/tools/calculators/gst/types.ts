import type { ToolConfig, ToolResult } from "@/backend/tools/calculation-tools";

export type GSTToolInput = Record<string, unknown>;
export type GSTToolConfig = ToolConfig;
export type GSTToolResult = ToolResult;
