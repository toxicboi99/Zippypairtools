import type { ToolConfig, ToolResult } from "@/backend/tools/calculation-tools";

export type BMIToolInput = Record<string, unknown>;
export type BMIToolConfig = ToolConfig;
export type BMIToolResult = ToolResult;
