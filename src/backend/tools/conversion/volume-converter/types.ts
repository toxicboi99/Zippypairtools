import type { ToolConfig, ToolResult } from "@/backend/tools/calculation-tools";

export type VolumeConverterToolInput = Record<string, unknown>;
export type VolumeConverterToolConfig = ToolConfig;
export type VolumeConverterToolResult = ToolResult;
